# Pantheon

A simple futuristic AI assistant interface built with React, Tailwind CSS, and Framer Motion.

Open `index.html` in a browser to try it out. Messages are sent to `http://localhost:3000/api/ask`.
